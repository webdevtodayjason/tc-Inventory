web: flask db init && flask db stamp head && flask db migrate && flask db upgrade && sleep 2 && flask create-category && sleep 2 && flask create-cpus && sleep 2 && flask create-tags && sleep 2 && flask create-admin --username="${ADMIN_USERNAME}" --email="${ADMIN_EMAIL}" --password="${ADMIN_PASSWORD}" --pin="${ADMIN_PIN}" && gunicorn --bind 0.0.0.0:8080 run:app 