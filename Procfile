web: flask db stamp head && flask db upgrade && flask create-category && flask create-cpus && flask create-tags && flask create-admin --username="${ADMIN_USERNAME}" --email="${ADMIN_EMAIL}" --password="${ADMIN_PASSWORD}" --pin="${ADMIN_PIN}" && gunicorn --bind 0.0.0.0:8080 run:app 